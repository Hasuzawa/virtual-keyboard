export const keyWidth = Object.freeze({
	STANDARD: 64,
	TAB_DEL: 98,
	CAP_RETURN: 117,
	SHIFT: 153,
	SPACEBAR: 352,
	COMMAND: 81,
})

export const padding = Object.freeze({
	default: 15,
	mac: 8,
	raspberry: 15,
})

export const smallerFont = Object.freeze({
	default: 14,
	mac: 14,
	raspberry: 14,
})

export const iconSize = Object.freeze({
	arrow: 30,
	window: 20,
	mac: 20,
	linux: 30,
	raspberry: 30,
})
